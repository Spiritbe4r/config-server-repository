spring:
  application:
    name: cardenascode-order
  datasource:
    password: 1234asdf
    url: jdbc:postgresql://localhost:5432/pizzeria
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
